# =============================================================================
# DataReady.io - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
APP_NAME=DataReady.io
APP_VERSION=0.1.0
DEBUG=false
HOST=0.0.0.0
PORT=8000

# -----------------------------------------------------------------------------
# Databricks AI Gateway (Required for AI features)
# -----------------------------------------------------------------------------
# Your Databricks workspace URL (e.g., https://your-workspace.cloud.databricks.com)
DATABRICKS_HOST=https://your-workspace.cloud.databricks.com

# Personal access token from Databricks
DATABRICKS_TOKEN=your-databricks-token-here

# Model endpoints (configured in your Databricks AI Gateway)
GEMINI_PRO_ENDPOINT=your-gemini-pro-endpoint
GEMINI_FLASH_ENDPOINT=your-gemini-flash-endpoint

# -----------------------------------------------------------------------------
# Whisper Speech-to-Text
# -----------------------------------------------------------------------------
# Set to true to use local Whisper model (requires more RAM)
# Set to false to use an API endpoint
USE_LOCAL_WHISPER=false

# Only needed if USE_LOCAL_WHISPER=false
WHISPER_API_ENDPOINT=https://your-whisper-api-endpoint

# -----------------------------------------------------------------------------
# Text-to-Speech
# -----------------------------------------------------------------------------
# Voice for Edge TTS (default: en-US-GuyNeural)
TTS_VOICE=en-US-GuyNeural

# -----------------------------------------------------------------------------
# Interview Settings
# -----------------------------------------------------------------------------
# Maximum questions per interview
MAX_INTERVIEW_QUESTIONS=10

# Maximum follow-up questions per core question
MAX_FOLLOWUPS_PER_QUESTION=2

# Starting difficulty (1-10)
DEFAULT_DIFFICULTY=5

# -----------------------------------------------------------------------------
# CORS Settings (comma-separated list of allowed origins)
# -----------------------------------------------------------------------------
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# =============================================================================
# Railway Deployment Notes:
# =============================================================================
# When deploying to Railway:
# 1. Add these as environment variables in Railway dashboard
# 2. Railway automatically sets PORT - don't set it manually
# 3. Set DEBUG=false for production
# 4. Ensure DATABRICKS_HOST and DATABRICKS_TOKEN are set
# =============================================================================

# -----------------------------------------------------------------------------
# Langfuse Observability (Optional but Recommended)
# -----------------------------------------------------------------------------
# Get your keys from https://cloud.langfuse.com
LANGFUSE_SECRET_KEY=sk-lf-your-secret-key
LANGFUSE_PUBLIC_KEY=pk-lf-your-public-key
LANGFUSE_BASE_URL=https://cloud.langfuse.com
LANGFUSE_ENABLED=true
